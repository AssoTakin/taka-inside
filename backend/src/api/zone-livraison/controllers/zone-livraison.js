'use strict';

/**
 * zone-livraison controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::zone-livraison.zone-livraison');
